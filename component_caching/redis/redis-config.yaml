apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
  namespace: redis-dev
data:
  redis.conf: |
    maxmemory 256mb
    maxmemory-policy allkeys-lru
    appendonly yes
    appendfsync everysec
    protected-mode no